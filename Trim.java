public class Trim {
    public static void main(String[] args) {
        String str = "   Hello World   ";
        System.out.println("Before trim: [" + str + "]");
        System.out.println("After trim: [" + str.trim() + "]");
// Clean extra spaces (start/end).

// Compare strings safely.

// Handle user input (like login forms).

// Correct length calculation.

// Clean data from files, DB, APIs.

// Detect empty inputs (spaces only).
    }
}
